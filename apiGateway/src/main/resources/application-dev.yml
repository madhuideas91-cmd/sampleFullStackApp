spring:
  application:
    name: apiGateway
  cloud:
    gateway:
      routes:
        - id: user-service-route
          uri: lb://USERSERVICE
          predicates:
            - Path=/users/**
        - id: task-service-route
          uri: lb://TASKSERVICE
          predicates:
            - Path=/tasks/**
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://77.37.47.79:3000"
              - "http://77.37.47.79"
              - "http://localhost:3000"
              - "http://localhost:5173"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://eureka-registry:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

logging:
  level:
    root: WARN
    org.springframework.cloud.gateway: INFO
